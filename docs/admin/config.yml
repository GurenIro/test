backend:
  name: git-gateway
  # Edit the repo
  # example: repo: fabricioanciaes/fexlguide
  # the repo name is what you've inserted when you clicked the netlify deploy button
  repo: gureniro/test
  branch: master

public_folder: "/images/"
media_folder: "/docs/images"
publish_mode: editorial_workflow


# Collections are categories in the side menu.
# It comes built in with General, Characters and System.
# The general ones are in the root, that means they arent in a submenu
# To create a new category you can just copy one of them and commit.
# Make sure identation is correct (YES, the number of spaces matter)

collections:
  - name: "General"
    label: "General"
    folder: "docs"
    create: true
    slug: "{{slug}}"
    fields:
    - {label: "Title", name: "title", widget: "string"}
    - {label: "Summary", name: "summary", widget: "string"}
    - {label: "Body", name: "body", widget: "markdown"}

  - name: "1 - Game Systems"
    label: "1 - Game Systems"
    folder: "docs/1-System"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}

  - name: "2 - Char Overview"
    label: "2 - Char Overview"
    folder: "docs/2-Overview"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}

  - name: "3 - Game Plans"
    label: "3 - Game Plans"
    folder: "docs/3-Gameplan"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}

  - name: "4 - Combos"
    label: "4 - Combos"
    folder: "docs/4-Combos"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}


  - name: "5 - Frame Data"
    label: "5 - Frame Data"
    folder: "docs/5-Framedata"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}
